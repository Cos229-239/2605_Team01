<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group>
        <item android:title="Calendar"
            android:id="@+id/calendar"
            android:icon="@drawable/hamburger_open_menu"
            tools:ignore="HardcodedText" />
        <item android:title="Task List"
            android:id="@+id/tasklist"
            android:icon="@drawable/tasklist_icon"
            tools:ignore="HardcodedText"/>
        <item android:title="Countdowns"
            android:id="@+id/countdowns"
            android:icon="@drawable/countdown_icon"
            tools:ignore="HardcodedText"/>
        <item android:title="Settings"
            tools:ignore="HardcodedText"
            android:id="@+id/settings"
            android:icon="@drawable/settings_icon"/>
    </group>

    <item android:title="Profile"
        tools:ignore="HardcodedText">
        <menu>
            <group>
                <item android:title="Login"
                    android:id="@+id/login"
                    android:icon="@drawable/login_icon"
                    tools:ignore="HardcodedText" />
            </group>
        </menu>
    </item>

</menu>